<Window x:Class="CalendarMVP.ConfirmClosingWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalendarMVP"
        mc:Ignorable="d"
        Title="ConfirmClosingWindowView" Height="400" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="50*"/>
        </Grid.RowDefinitions>
        <TextBlock TextAlignment="Center" FontSize="30" VerticalAlignment="Center">Are you sure you want to close the window?<LineBreak /> Any unsaved changes will be lost.</TextBlock>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50*"/>
                <ColumnDefinition Width="50*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Margin="50 50 20 50" FontSize="30">Cancel</Button>
            <Button Grid.Column="1" Margin="20 50 50 50" FontSize="30">Ok</Button>
        </Grid>
    </Grid>
</Window>
